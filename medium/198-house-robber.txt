# 198. House Robber
# Difficulty: Medium
# URL: https://leetcode.com/problems/house-robber/
# Date: 07/04/2026

# ========== Solution A ==========
# Language: txt
# Date: 07/04/2026

class Solution {class Solution {
    public int rob(int[] nums) {    public int rob(int[] nums) {
        int start = 0;        int start = 0;
        int end = nums.length -1;        int end = nums.length -1;
                
    }    }
    private int robMax(int[] nums, int start, int end){    private int robMax(int[] nums, int start, int end){
        if(start == end) return nums[start];        if(start == end) return nums[start];
    }    }
        return robMax(nums,start,end);        return robMax(nums,start,end);
        else return Math.max(nums[start]+robMax(nums,start+2,end), robMax(nums,start+1,        else return Math.max(nums[start]+robMax(nums,start+2,end), robMax(nums,start+1,
        // max between robbing current and not robbing current        // max between robbing current and not robbing current
        // current being the first one        // current being the first one
end));end));
        if(start>end) return 0; // prevent going out of bounds        if(start>end) return 0; // prevent going out of bounds
}}


